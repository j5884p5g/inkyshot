[tool.poetry]
name = "inkyshot"
version = "0.1.0"
description = "exploit"
authors = ["Exploiter <exploiter@example.com>"]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.group.dev.dependencies]
pytest = "^7.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "inkyshot"
version = "0.1.0"
description = "Inkyshot"
authors = ["Author <author@example.com>"]
packages = [{include = "inkyshot"}]

[tool.poetry.dependencies]
python = "^3.8"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
